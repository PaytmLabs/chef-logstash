output {
  elasticsearch {
<% if @elasticsearch_ip -%>
    hosts => ["<%= @elasticsearch_ip %>"]
<% else -%>
    hosts => ["127.0.0.1"]
<% end -%>
    protocol => "http"
<% if @es_index -%>
    index => "<%= @es_index %>"
<% end -%>
  }
}
